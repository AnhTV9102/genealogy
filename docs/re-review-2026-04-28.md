# Re-review Report (Round 3)

Date: 2026-04-28  
Checked commit: `cf3134e`  
Scope: Coding, `docs/`, `database/`

## Conclusion

After re-reviewing the current branch state, the previously reported issues are still present in code and SQL/docs. This report is based on direct file inspection at commit `cf3134e`.

## Findings (Current State)

### 1) Relationship type canonicalization is still inconsistent (**Open**)

- Service parses uppercase enum value but duplicate check still uses raw request input.
- Repository queries use uppercase literals (`PARENT_OF`, `SPOUSE_OF`).
- SQL init indexes still filter on lowercase values (`parent`, `spouse`).

**Evidence**
- `src/main/java/com/genealogy/relationship/application/service/RelationshipService.java`
- `src/main/java/com/genealogy/relationship/infrastructure/persistence/repository/JpaRelationshipRepository.java`
- `database/init/init.sql`

### 2) Service abstraction leak remains in `PersonService` (**Open**)

- `PersonService` still depends on `PersonRepositoryImpl` directly instead of only domain repository abstraction.

**Evidence**
- `src/main/java/com/genealogy/person/application/service/PersonService.java`

### 3) Raw `RuntimeException` usage remains (**Open**)

- Not-found and duplicate conditions still throw `RuntimeException` directly.

**Evidence**
- `src/main/java/com/genealogy/person/application/service/PersonService.java`
- `src/main/java/com/genealogy/relationship/application/service/RelationshipService.java`

### 4) Request validation annotations still missing (**Open**)

- Controllers still accept request DTOs without `@Valid`.
- Request DTO records still do not define bean validation constraints.

**Evidence**
- `src/main/java/com/genealogy/person/api/PersonController.java`
- `src/main/java/com/genealogy/relationship/api/RelationshipController.java`
- `src/main/java/com/genealogy/person/application/dto/CreatePersonRequest.java`
- `src/main/java/com/genealogy/relationship/application/dto/CreateRelationshipRequest.java`

### 5) `docs/database/erd.md` is still empty (**Open**)

**Evidence**
- `docs/database/erd.md`

## Recommended next fixes

1. Canonicalize `RelationshipType` value handling end-to-end (`type.name()` in service/repo + SQL index filters + docs).
2. Move pagination API to `PersonRepository` interface and remove `PersonRepositoryImpl` dependency from service.
3. Introduce typed exceptions and map them via `@ControllerAdvice`.
4. Add `@Valid` and bean validation constraints in request DTOs.
5. Populate `docs/database/erd.md` and keep docs/schema/init aligned.
