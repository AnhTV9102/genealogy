# Re-review Report (Round 2)

Date: 2026-04-28  
Scope: Re-review of coding, `docs/`, and `database/` after reported fixes.

## Result

I re-checked the same high-priority items from the previous review. In the current repository state, those items are still present and appear **not yet changed**.

## Status vs Previous High-Priority Findings

| ID | Item | Current Status | Evidence |
|---|---|---|---|
| C4 | Relationship type normalization mismatch in duplicate check | **Open** | `RelationshipService#create` still uses `request.type()` for duplicate check while enum parsing uses uppercased value |
| D2 / DB1 | SQL/docs/code mismatch for relationship type literals | **Open** | `init.sql` still uses `'parent'`/`'spouse'`, while code queries use `'PARENT_OF'`/`'SPOUSE_OF'` |
| D1 | Empty ERD markdown page | **Open** | `docs/database/erd.md` still has 0 lines |

## Additional Previously Reported Items (Still Open)

- **C1 (service abstraction leak):** `PersonService` still depends on both `PersonRepository` and `PersonRepositoryImpl`.
- **C2 (generic exceptions):** `RuntimeException` is still used for not-found/duplicate flows.
- **C3 (request validation):** Controllers still accept request DTOs without `@Valid`, and DTO constraints are still absent.

## Practical Next Steps

1. **Canonicalize relationship type values end-to-end**
   - Use `RelationshipType` enum names consistently in service checks, DB storage, SQL constraints, and docs.
2. **Fix SQL partial indexes**
   - Update filtered indexes/constraints in `database/init/init.sql` to match canonical stored values.
3. **Add API validation and typed errors**
   - Add bean validation annotations to request records + `@Valid` in controllers.
   - Replace raw `RuntimeException` with domain/application exceptions + centralized exception handler.
4. **Complete database docs**
   - Populate `docs/database/erd.md` and keep it aligned with schema and mermaid ERD.

## Notes

If you already made fixes locally, they are not visible in this checked branch state yet. Please push/merge those changes and I can run another re-review immediately.
